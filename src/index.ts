main(process.argv);

//TODO: This does not need testing. It is simply parsing the input.
function main(argv: string[]) {
    console.log(argv.length);
    console.log(argv[0]);
    console.log(argv);
}

